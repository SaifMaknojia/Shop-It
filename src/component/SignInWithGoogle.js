import React from 'react';
import { Button } from 'react-bootstrap';

const SignInWithGoogle = () => {
  return (
    <Button className="mt-4 flex-grow" variant="primary" type="submit">
      Sign In With Google
    </Button>
  );
};

export default SignInWithGoogle;
